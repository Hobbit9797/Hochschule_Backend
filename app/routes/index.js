const personRoutes = require('./PersonRoutes');
const studiumRoutes = require('./StudiumRoutes');

module.exports = {
    personRoutes,  // Other route groups could go here, in the future};
    studiumRoutes
}
